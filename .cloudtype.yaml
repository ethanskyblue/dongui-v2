# Cloudtype 배포 설정
# https://cloudtype.io

# 빌드 및 실행 설정
build:
  command: npm install
start:
  command: node server.js

# 포트 설정
port: 3000

# 환경변수 (Cloudtype 대시보드에서 설정)
# ANTHROPIC_API_KEY=sk-ant-...
# ALLOWED_ORIGINS=https://your-app.cloudtype.app
